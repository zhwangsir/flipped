# flipped · 缺陷日志（DEFECT_LOG.md）

> 循环测试过程中发现的所有缺陷流水。格式见 TEST_PLAN.md §5。
> 每条缺陷结构化记录，便于趋势分析与复发检测。

| ID | 轮次 | 级别 | 类型 | 现象 | 根因 | 修复 | 验证 | 状态 | 复发 |
|---|---|---|---|---|---|---|---|---|---|
| D-0001 | R1 | P1 | 回归 | quality_gate.sh L82/84/129/131 + loop_test.sh L85/103/112 共 7 处 `$VAR<全角字符>` 触发 `set -u` 下 `PY_FAILED\xxx: unbound variable`，门禁误判失败（实际 pytest 1639 passed/0 failed/82.85% cov 全绿） | M144-A 节警告过的陷阱复发：bash 在某些 locale 下把 `$IDENT` 后的多字节字符（`）`、`，`）当作变量名一部分 | 7 处统一改 `${VAR}` 显式大括号；新增 `scripts/check_shell_lint.sh` 守卫接入 G0 门禁；同顺手修 await_glm_capstone.sh L21 同类 | `bash scripts/check_shell_lint.sh` 28 文件全绿 | fixed | 0（M144-A 警告过 → 本次为复发，已加自动化守卫根治） |
| D-0002 | 安全基线 | P1 | 安全 | security_scan.sh S1/S2/S3 三项扫描全部误报「未安装或执行失败，跳过」，实际工具已装在 .venv/bin/ 但退出码非 0 被误判 | bandit/pip-audit/npm audit 三个工具发现 issue 时退出码=1（语义=「有发现」），脚本用 `if $TOOL ...` 判断退出码会把「成功扫描但发现问题」误判为「未安装」；npm audit 另叠加镜像源问题（registry.npmmirror.com 不支持 audit endpoint 返回 404） | 改用「JSON 报告生成且可解析」作为执行成功条件（`[ -f reports/X.json ] && python -c "json.load(...)"`）；npm audit 显式指定 `--registry=https://registry.npmjs.org` | `bash scripts/security_scan.sh` 三项工具全部正常执行并产出真实发现 | fixed | 0 |
| D-0003 | 安全基线 | P2 | 安全 | bandit B324: 12 处 `hashlib.md5()` 被标记为 HIGH 级弱哈希（adaptive_loop/design_context/failure_kb/gold_memory/knowledge_preflight/skill_registry/stuck_detector/visual_feedback/visual_regression） | 全部为缓存键/指纹生成用途（取 hexdigest 前 8-16 位作短哈希），非密码学用途；bandit 默认对所有 hashlib.md5 报警 | 批量添加 `usedforsecurity=False` 参数（Python 3.9+ 标准），用 `scripts/fix_b324_md5.py` 脚本带括号深度计数批量修复 9 文件 12 处 | `bandit -r src/ --severity-level high` → 0 HIGH（从 13 降至 0）；9 文件语法验证通过；61 回归测试 passed | fixed | 0 |
| D-0004 | 安全基线 | P2 | 安全 | bandit B602: orchestrator.py:1296 `subprocess.run(command_str, shell=True, ...)` | shlex.split 安全解析失败时的回退路径，命令需要 shell 特性（管道/重定向） | 审查确认：command_str 在执行前已过 `is_safe_command()` 白名单审查 + `classify_risk()` 风险分级（high 级直接拒绝）；加 `# nosec B602` 显式标记已审查 + 注释说明安全保障链 | `bandit --severity-level high` → 0 HIGH；B602 清零 | fixed | 0 |
| D-0005 | 安全基线 | P1 | 安全 | pip-audit: 13 个 Python 依赖有 60 个漏洞（pillow 20、pyjwt 9、gitpython 8、mcp 3、pyasn1 3、python-multipart 3、chromadb/cryptography/diskcache/pypdf2 各 1） | 依赖版本未及时升级，部分漏洞有上游修复版本可用 | 分两批升级：① 低风险（patch/minor）gitpython 3.1.57、pillow 12.3.0、pyasn1 0.6.4、pyjwt 2.13.0、python-multipart 0.0.32；② 中风险 mcp 1.28.1、python-socketio 5.16.3、setuptools 83.0.0、cryptography 49.0.0。消除 50 漏洞（83.3%）。剩余 10 漏洞/4 依赖：chromadb+diskcache 无修复版本(wontfix)、pypdf2 PyPI 无 3.9.0(wontfix 传递依赖)、starlette 受 FastAPI<0.51.0 约束需协同升级(暂缓) | `pip-audit` → 10 漏洞（从 60 降至 10，83.3% 消除）；全量回归 1748 passed/0 failed | fixed | 0 |
| D-0006 | 安全基线 | P2 | 安全 | npm audit: 10 个前端依赖漏洞（critical 2、high 5、moderate 3），根因两个 dev dependency 链：brace-expansion(high DoS) 需 @vitest/coverage-v8 4.x + esbuild(moderate dev server) 需 vite 8.x | 前端测试/构建工具链版本未及时升级 | 执行 major 升级：vite 5.4.11→8.1.5（Rolldown 引擎替代 esbuild+Rollup）+ vitest 2.1.9→4.1.10 + @vitejs/plugin-react 4.3.4→6.0.4（v6 移除 Babel 用 Oxc）+ @vitest/coverage-v8 2.1.9→4.1.10；vitest.config.ts 添加 coverage.include（Vitest 4 移除 coverage.all，不显式 include 则未加载文件不计入分母）；修复 2 处 WebSocket mock 箭头函数构造函数 vi.fn(()=>ws)→vi.fn(function(){return ws})（Vitest 4 禁止箭头函数做构造函数，缺 [[Construct]]） | `npm audit --registry=https://registry.npmjs.org` → 0 vulnerabilities（10→0）；tsc --noEmit 0 错；vitest 591/591 passed；vite build 93ms 成功（Rolldown）；coverage 门禁 Lines 90.25%/Branches 82.84% 全过 | fixed | 0 |
| D-0007 | a11y 扫描 | P3 | 无障碍 | axe-core 扫描 Assistant/Factory 视图发现 `landmark-one-main`（html 缺 main landmark，1 处）+ `region`（内容未包含在 landmark 内，Assistant 7 处 / Factory 11 处 / Factory 新建表单 15 处）+ `landmark-unique`（.topbar 与 .factory-head 都是 `<header>`，landmark 重复，Factory 视图 1 处）共 3 类 moderate 违规 | `App.tsx` 用 `<section className="view-assistant">` 而非 `<main>`；`FactoryPanel.tsx` 的 `.factory-head` 用 `<header>` 与 `TopBar.tsx` 的 `<header className="topbar">` 冲突；body 直接子元素未包进 landmark | 修复：① `Assistant.tsx` L135 `<section className='view-assistant'>` → `<main>`（+ L154 `</section>` → `</main>`），Assistant 视图成为页面 main landmark；② `FactoryPanel.tsx` L113 `<header className="factory-head">` → `<div className="factory-head">`（+ L128 `</header>` → `</div>`），消除与 TopBar 的 landmark-unique 冲突；③ `FactoryPanel.tsx` L104-109 给 `.factory-overlay` 加 `role="dialog" aria-modal="true" aria-label="工厂面板"`，把 FactoryPanel 内容包进 dialog 上下文（同时满足 aria-dialog-name） | `npx playwright test accessibility/`：landmark-one-main 0 违规（原 1）、landmark-unique 0 违规（原 1）；region：Assistant 7→3（剩 3 在 Sidebar/ContextPanel，本任务范围外）、Factory 11→10（剩 10 在 Conversation/ContextPanel，本任务范围外）、Factory 新建表单 15→10（同上）。accessibility 20/20 通过；vitest 591/591 0 退化；兼容性 62/62 0 退化；tsc 0 errors | fixed | 0 |
| D-0008 | a11y 扫描 | P3 | 无障碍 | axe-core 扫描 Factory 新建表单发现 `heading-order` 违规 1 处：h3 缺 h2 前置 | FactoryPanel 新建表单用 `<h3>` 作为首级标题，但页面无 `<h1>`/`<h2>` 前置 | 修复：`FactoryPanel.tsx` L203 `<h3 className="factory-create-title">新建工厂</h3>` → `<h2 className="factory-create-title">新建工厂</h2>`。h2 成为面板内首级标题，无跳级 | `npx playwright test accessibility/`：heading-order 0 违规（原 1）；accessibility 20/20 通过；vitest 591/591 0 退化；tsc 0 errors | fixed | 0 |
| D-0009 | a11y 扫描 | P3 | 无障碍 | 装饰性 SVG 未设 `aria-hidden="true"`：`console/src/icons.tsx` 全部 26 个内联 SVG 图标均无 `aria-hidden`，屏幕阅读器可能读出 `<path>` 数据 | `icons.tsx` 的 `b()` 工厂函数未给 SVG 加 `aria-hidden="true"`；项目用自定义内联 SVG（非 Lucide React），需手动补属性 | `icons.tsx` 的 `b()` 工厂函数返回对象加 `aria-hidden: "true"`，一行覆盖全部 43 个内联 SVG 图标（IconPlus…IconRefresh + ToolIcon 分发）；未迁移 Lucide React（属另一待决策事项） | `npx playwright test accessibility/aria.spec.ts` 的「装饰性 SVG 应 aria-hidden」由软断言升级为硬断言 `decorativeWithoutHidden===0` 通过；`accessibility/` 全 20 测试绿；`vitest run` 591/591 0 退化 | fixed | 0 |
| D-0010 | a11y 扫描 | P2 | 无障碍/键盘 | `Settings.tsx` 抽屉未监听 Escape 键，用户无法通过键盘 Escape 关闭设置面板（只能点击关闭按钮/遮罩） | `Settings.tsx` 无 `useEffect` 绑定 document keydown Escape；对比 `CommandPalette.tsx` L94-96 有 Escape 处理，`Sidebar.tsx` L59-61 项目菜单也有，Settings 漏了 | `Settings.tsx` 加 `useEffect` 在 `settingsOpen===true` 时绑 `document keydown Escape → setSettingsOpen(false)`，卸载时解绑；与 Sidebar L54-68 项目菜单同模式（document 级监听，无需焦点落在面板内） | `e2e/accessibility/keyboard-nav.spec.ts` 的「Escape 能关闭设置抽屉」`test.fail()` 已移除恢复为正常 `test`，断言通过；`accessibility/` 全 20 测试绿；`vitest run` 591/591 0 退化 | fixed | 0 |
| D-0011 | a11y 扫描 | P3 | 无障碍/键盘 | `Composer` 的 textarea 在第 14 次 Tab 停点无可见 focus-visible 样式（`[kb:focus-visible]` 警告） | `assistant-composer-input` 的 CSS 未显式定义 `:focus-visible` 样式，或被 `outline: none` 覆盖 | 修复：`console/src/styles/app.css` L5696-5703 新增 `.assistant-composer textarea:focus-visible { outline: 2px solid var(--assistant-indigo); outline-offset: -2px; }`。选择器特异性 (0,2,1) 高于全局 `textarea:focus { outline: none }` (0,1,1)，键盘 Tab 停点可见焦点环。与 `.assistant-tool .tool-head:focus-visible` 同色 | `npx playwright test accessibility/keyboard-nav.spec.ts`：测试 15「焦点可见」通过，无 `[kb:focus-visible]` 警告日志（原第 14 次 Tab 停点警告消除）；accessibility 20/20 通过；vitest 591/591 0 退化 | fixed | 0 |
| D-0012 | a11y 扫描 | P3 | 无障碍/键盘 | FactoryPanel overlay 未监听 Escape 键，用户无法通过键盘 Escape 关闭工厂面板（只能点击关闭按钮） | `FactoryPanel.tsx` 无 document keydown Escape 监听；与 Settings 同模式缺陷 | 修复：`FactoryPanel.tsx` L10 import 加 `useEffect`；L54-67 新增 `useEffect`，`factoryOpen===true` 时绑 `document keydown Escape → navigate("assistant"); setFactoryOpen(false)`，卸载时解绑。与 Settings.tsx D-0010 修复同模式（document 级监听，无需焦点落在面板内） | `npx playwright test accessibility/keyboard-nav.spec.ts`：测试 19「Escape 能关闭 Factory overlay」通过，无 `[kb:factory-escape] 警告` 日志（Escape 直接关闭，不再走 Tab+Enter 回退路径）；accessibility 20/20 通过；vitest 591/591 0 退化 | fixed | 0 |

| D-0013 | 性能基线 | P2 | 性能/前端 | Lighthouse 扫描 Assistant 视图 FCP=2300ms 超过 1800ms 阈值（spec TEST_STRATEGY_OPTIMIZATION.md §2.4） | 根因修正：Geist Sans 字体（@fontsource-variable/geist）在 main.tsx 被 import 但 tokens.css 的 --font-sans 用系统字体栈，Geist Sans 从未被任何 font-family 引用 —— 5 个 woff2 子集（~75KB）+ @font-face CSS 规则是纯死重，被打进 entry chunk 拖慢 FCP。原诊断"未设 font-display: swap"不准确：fontsource 包已自带 swap，但_UNUSED_字体的 woff2 + @font-face 规则仍在 entry CSS/JS 中 | 修复：`console/src/main.tsx` 移除 `import "@fontsource-variable/geist"`（Geist Sans 未使用）；保留 `@fontsource-variable/geist-mono`（用于 --font-mono，fontsource 已自带 font-display: swap） | Lighthouse 复测：Assistant FCP 2269ms→1783ms（-21.4%），<1800ms 阈值达标。证据见 TEST_LOG.md "D-0013/D-0014 前端性能优化" | fixed | 0 |
| D-0014 | 性能基线 | P2 | 性能/前端 | Lighthouse 扫描 Factory 视图 LCP=3472ms 超过 2500ms 阈值 + FCP=2289ms 超过 1800ms 阈值 | Factory 视图含 xterm 终端 + 多面板，bundle 更重；与 Assistant 共享同一 entry chunk，无路由级 code-split | 修复（三层 code-split）：① `console/src/App.tsx` 用 React.lazy + Suspense 懒加载 FactoryPanel + Conversation（Factory 路由专属），FactoryPanel 额外用 factoryOpen 外部门控（Assistant 首屏不加载 Factory chunk）；② `console/src/components/PtyTerminal.tsx` 将 @xterm/xterm + @xterm/addon-fit 改为 dynamic import（import type + useEffect 内 await import），xterm JS ~290KB 移出 entry chunk，仅在终端首次激活时加载；③ 同步 D-0013 字体修复。Entry JS 536KB→219KB（-59%） | Lighthouse 复测：Factory FCP 2249ms→1732ms（-23.0%），LCP 3448ms→1732ms（-49.8%），均达阈值。vitest 591 passed / Playwright 兼容性 62 passed 0 退化。证据见 TEST_LOG.md | fixed | 0 |
| D-0015 | M157 覆盖率补测 | P2 | 功能/数据 | `src/driving/failure_kb.py:345` `get_all_failures` 中 `ORDER BY created_at DESC` 引用不存在的列（failures 表 schema 只有 `timestamp`，无 `created_at`），导致 `cursor.execute` 永远抛 `sqlite3.OperationalError` 被 fail-open 捕获返回 `[]`。连带影响：`run_clustering_analysis`（L372 调用 `get_all_failures`）永远收到空列表 → 永远返回 `has_data: False`，聚类分析功能实际不可用。覆盖率补测时发现（L348-349 不可达） | `get_all_failures` 写 SQL 时列名写错，应为 `timestamp` 而非 `created_at`；该函数可能从未被真实使用过（否则聚类失效会被发现）；测试 `TestGetAllFailures` 旧版把 buggy 行为（`== []`）当预期硬编码，违反 §3 红线 | 修复：① `src/driving/failure_kb.py:345` `ORDER BY created_at DESC` → `ORDER BY timestamp DESC`（与 schema L37 + L218 一致）；② 重写 `tests/test_failure_kb.py::TestGetAllFailures` 7 个测试断言正确行为（返回真实数据、resolved 过滤、limit、timestamp 倒序、异常 fail-open）；③ 新增 3 个 D-0015 回归守卫：`test_orders_by_timestamp_desc_newest_first`、`test_d0015_end_to_end_clustering_works`（写数据→get_all_failures 非空→run_clustering_analysis has_data=True）、`test_has_data_when_real_failure_recorded` | `PYTHONPATH=src python -m pytest tests/test_failure_kb.py tests/test_failure_clustering.py -v` → 53 passed in 3.00s（含 4 个新回归守卫全绿）；旧 bug 下 `test_d0015_end_to_end_clustering_works` 会断言失败（`len(entries)==0 != 3`），证明守卫有效 | fixed | 0 |
