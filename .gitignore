# ---- 密钥 / 凭据（绝不入库，AGENTS.md §7）----
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# ---- Node ----
node_modules/
dist/
build/
*.log
npm-debug.log*
pnpm-debug.log*

# ---- Python ----
__pycache__/
*.pyc
.venv/
.venv*/
venv/
.pytest_cache/

# ---- 运行时 / 产物 ----
logs/
*.tmp
*.bak
*.bak2
.cache/
# SQLite 运行时 db(checkpoints/e2e/长跑测试残留) - 不入库
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# ---- OS / 编辑器 ----
.DS_Store
.idea/
.vscode/

# SearXNG 运行配置(含 secret)
infra/searxng/settings.yml

# Cline CLI 隔离状态(含 API key)
.cline-data/

# Cline hook 审计日志(运行时)
src/driving/hooks/audit.jsonl
logs/cline-audit.jsonl

# IDE 控制面扩展构建产物
ide-extension/out/
console/tsconfig.tsbuildinfo
console/test-results/
console/playwright-report/
data/rag/

.sessions.json
.sessions.perf.json
.devlogs/
projects/
desktop/node_modules/
desktop/release/

# M7.3 MCP 运行时开关状态(首次运行自动生成种子)
config/mcp_servers.json

# M137 迁移备份(二进制, 不入库)
data/*.bak-*
data/*.db.bak.*

# M173/M183 运行时产物(可再生, 不入库)
data/project_maps/
data/worker_rule_stats.json

# M186 评审历史落盘(运行时产物)
data/reviews/

# ---- 测试 / 覆盖率 / 质量门禁产物（M153） ----
.coverage
.coverage.*
coverage.xml
coverage.json
htmlcov/
console/coverage/
quality_metrics.json
quality_metrics_history.jsonl

# ---- M157.10 结构化失败信号产物（quality_gate.sh 每次运行清空+重写） ----
reports/findings.jsonl

# ---- 心跳监控报告产物（M153） ----
# 脚本本身入库，运行时生成的报告/日志不入库
reports/heartbeat_*.md
reports/heartbeat.log
reports/heartbeat_history.jsonl
reports/heartbeat_loop.log
reports/recovery_*.md

# ---- 循环测试常态化产物（M154.1） ----
reports/cyclic_test_loop.log
reports/cyclic_test_history.jsonl

# ---- 性能测试产物（TEST_STRATEGY_OPTIMIZATION.md §2） ----
reports/perf/

# ---- M164 补: 心跳/安全扫描/测试报告产物（生成物不入库） ----
reports/stuck_*.md
reports/e2e_*.md
reports/bandit.json
reports/npm-audit.json
reports/pip-audit.json
reports/secrets.json
reports/benchmark.json
reports/coverage_full.json
reports/lighthouse-*.json
console/reports/

# ---- Trae 编辑器产物 ----
.trae/
