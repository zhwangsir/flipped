# 集群清单

> 最后更新: 2026-07-30 17:00(pc02 修复完成 + ComfyUI-LB 恢复 5 后端 + 全设备远程连通性确认)
> 维护者: 设备管家(AI Assistant)
> 用途: 所有项目 AI 和管理会话的单一真相源
> 本文件已分发到所有项目目录,统一命名为 `设备说明.md`

---

## 0、近期变更记录

### 2026-07-30 17:00 pc02 修复完成 — ComfyUI-LB 恢复 5 后端

| 变更项 | 旧状态 | 新状态 | 影响 |
|--------|--------|--------|------|
| pc02 ComfyUI :8193 | ❌ 离线(PyTorch cu124 不支持 sm_120) | **✅ 在线**：PyTorch 升级至 2.13.0+cu130 + extra_model_paths.yaml 缩进修复 | ComfyUI-LB 恢复 5 后端(gpu0/1/2 + pc01 + pc02) |
| pc02 extra_model_paths.yaml | YAML 缩进错误(`latent_upscale_models` 2 空格)导致 ComfyUI 启动崩溃 | **修复为 4 空格缩进**，从 pc01 同步正确配置 | pc02 启动正常加载 nas+toiv 双模型路径 |
| pc02 Tailscale | NoState 需要 GUI 登录 | ❌ 仍离线（SSH 无法完成浏览器认证） | 不影响使用：pc02 通过 LAN 接入 ComfyUI-LB；远程通过 workstation Tailscale 间接访问 |
| spark02 Tailscale | 离线 | ❌ 仍离线 | 不影响使用：spark01/02 通过 LAN NCCL 通信(TP=2)，spark01:8000 vLLM 正常服务 llama-3.3-70b |

### 2026-07-30 14:30 ToIV 业务上线 + pc02 修复 + style_presets LoRA 修复

| 变更项 | 旧状态 | 新状态 | 影响 |
|--------|--------|--------|------|
| core ToIV 业务 | 待迁移 | **toiv-api :8090 + toiv-web :3100 已 active** | ToIV 生产应用层运行在 core，PG18/Redis/NAS 挂载全就位 |
| spark 模型 | euryale-70b | **llama-3.3-70b-abliterated** | AGENTS.md 中 Euryale 信息已过时，实际模型为 llama-3.3-70b-abliterated TP=2 |
| L2/L3 EXO 层 | studio EXO 正常 | **studio01-04 全部 Tailscale 离线，L2/L3 临时降级走 spark** | 精修质量暂时受 L1 限制；出门期间无法远程恢复 studio Tailscale |
| Workstation LiveAct | 无 | **LiveAct worker :9400(GPU1+2)，SoulX-LiveAct 14B FP4 双卡，全身数字人 4-6FPS** | 新增数字人直播能力 |
| OpenTalking 驱动 | QuickTalk | **MuseTalk**（更正） | SenseVoice STT + MuseTalk 驱动 |
| pc02 ComfyUI | ✅ :8193 在线 | **❌ :8193 离线(PyTorch 2.6+cu124 不支持 RTX 5090 sm_120)，PyTorch cu130 升级修复中** | ComfyUI-LB 实际 4 个后端（缺 pc02），P1 并发化提速预期按 4 worker 算 |
| style_presets.py LoRA 语法 | 10 个预设用 `<lora:xxx>` A1111 语法（ComfyUI 不识别，无效） | **修复为 ComfyUI LoraLoader 节点加载**：StylePreset 新增 `loras` 字段，generate.py 自动合并；LoRA 使用带子目录的正确文件名；LTX 视频 LoRA 不给图像工作流用（避免报错） | 7 个 FLUX/SDXL 兼容 LoRA 正式生效，3 个架构不兼容的仅保留文字提示 |
| ComfyUI-LB 后端 | 5 个(gpu0/1/2 + pc01/pc02) | **临时 4 个**（pc02 离线） | pc02 修复后恢复 5 个 |

### 2026-07-30 早间 — LTX 上线 + worker 模型可见 + WebRTC TURN

| 变更项 | 旧状态 | 新状态 | 影响 |
|--------|--------|--------|------|
| LTX-2.3 独立板块 | 仅 NSFW 区有 LTX(固定 10eros) | **LTX 工作室板块上线**:dev bf16/distilled-1.1 满血下载(43G×2)+ 蒸馏提速 LoRA + 时空上采样 + Union/MotionTrack IC-LoRA + Cameraman v2 运镜 LoRA;后端 /api/ltx2/*(白名单/LoRA 注入/NSFW 门槛),core E2E 实测 22B 出片 h264 | LTX-2.3 成为独立主力视频线,与 Wan 2.2 并行不冲突 |
| worker 新模型接入 | toiv/comfyui-models 对 worker 不可见 | **5 台 worker extra_model_paths 全部生效**(workstation gpu0-2 + pc01/pc02) | Qwen-Image 2.0 编码器/42 场景 LoRA/LTX-2.3 全可见;PuLID 节点仍待装 |
| 数字人 WebRTC | ICE 全挂(浏览器 mDNS host candidate 跨子网不可解析,srflx 公网候选不可用) | **workstation 部署 coturn TURN 中继(:3478),opentalking ice-config 下发 STUN+TURN,policy=all** | aiortc 客户端 E2E 实测 CONNECTED,音视频轨全通(.31 网段→.71) |
| ToIV P0 模型下载 | 部分缺失 | **Qwen3-VL-8B-Instruct + PuLID Flux + EVA02-CLIP-L + IPAdapter FaceID Plus v2 + ACE-Step 1.5 全部下载完成**（NAS `toiv/comfyui-models`，2026-07-30 逐项核实） | ⚠️ 仅下载落盘，**worker 尚不可见**，详见 2.5 节「ToIV 新模型接入缺口」 |
| ToIV P1 场景 LoRA 下载 | 已有 30+ 个 | **新增 42 个场景 LoRA（约 14GB，实测 10 个分类目录合计 ≈13.8GB）**，覆盖古风/都市/校园/豪车/特效/恐怖/喜剧/历史战争/运镜/导演风格 | ⚠️ 同上，worker 不可见，待接入 |
| ToIV P1 音频分离 | 无 | **Demucs v4(htdemucs) + MDX-Net ONNX 部署测试通过；UVR5 模型已下载** | 人声/伴奏/音效分离可用（workstation `/home/merlin/toiv-scripts/audio-sep-venv`，CLI 可用） |
| ToIV 代码配置 | Qwen-Image 1.0 编码器兜底 | **model_profiles.py 更新 Qwen-Image 2.0 编码器候选；style_presets.py 新增 10 个场景 LoRA 预设** | ⚠️ 代码改动在工作区**未提交**；且引用的模型 worker 不可见，需先完成接入再提交 |

### 2026-07-30 外出前连通性核查

> 从 MateBook（Tailscale 100.74.15.34）检查所有设备 Tailscale 可达性。

| 设备 | Tailscale IP | 可达 | 备注 |
|------|--------------|------|------|
| core | 100.77.80.100 | ✅ | LAN 直连 |
| workstation | 100.68.100.90 | ✅ | LAN 直连 |
| spark01 | 100.81.235.124 | ✅ | LAN 直连 |
| spark02 | 100.86.42.89 | ✅ | LAN 直连 |
| studio01 | 100.67.43.40 | ❌ | Tailscale 离线，最后可见 1m |
| studio02 | 100.91.0.121 | ❌ | Tailscale 离线，最后可见 6d |
| studio03 | 100.115.27.68 | ❌ | Tailscale 离线，最后可见 2d |
| studio04 | 100.126.182.23 | ❌ | Tailscale 离线，最后可见 6d |
| openclaw01 | 100.115.23.67 | ✅ | LAN 直连 |
| openclaw02 | 100.76.35.7 | ✅ | LAN 直连 |
| openclaw03 | 100.76.140.121 | ✅ | LAN 直连 |
| openclaw04 | 100.125.217.11 | ✅ | LAN 直连 |
| pc01 | 100.69.134.27 | ✅ | LAN 直连 |
| pc02 | 100.107.94.26 | ✅ | LAN 直连 |
| nas | 100.80.237.96 | ✅ | LAN 直连 |
| cloud | 100.83.78.114 | ✅ | 经 DERP 中继 |

**关键服务状态**：Nemotron :8000、ComfyUI-LB :8188、TTS :9200、**ASR :9210**、Embedding :9302、OpenTalking :4403、Spark :8000、NAS SMB :445、Cloud HTTPS aigc.dgmt.top、Cloud frps :7500 **全部正常**。

**外出访问建议**：
- 随身携带设备上保持 Tailscale 运行，即可访问所有 ✅ 设备，并正常使用核心服务。
- Mac Studio 全部 Tailscale 离线，出门后无法远程 SSH/EXO 访问；如需要，请到家后在每台 Studio 上手动启动 Tailscale App。
- ASR :9210 已修复（CUDA 库 + 离线启动），可正常转写。

### 2026-07-29

| 变更项 | 旧状态 | 新状态 | 影响 |
|--------|--------|--------|------|
| AI-Omni ASR | 无 | **Workstation :9210 faster-whisper large-v3 真机运行** | omni_voice ASR 缺口补齐,OpenAI 兼容 /v1/audio/transcriptions |
| Workstation GPU2 | 仅 ComfyUI #3 (~0.5GB) | **+ AI-Omni ASR (~4.9GB)** | GPU2 显存占用约 5.4GB,仍有 92GB 空闲 |

### 2026-07-28

| 变更项 | 旧状态 | 新状态 | 影响 |
|--------|--------|--------|------|
| Workstation Docker | 17 容器 | **0 容器(全清)** | 所有业务服务转真机,避免 Docker 额外消耗 |
| Workstation 内存 | 20Gi used | **12Gi used** | 清理 /tmp 残留 8G,释放内存 |
| Workstation Nemotron vLLM | `--enforce-eager`, 显存 88% | **去掉 `--enforce-eager`,启用 chunked-prefill/prefix-caching,显存 95%** | 吞吐提升,更多 KV cache |
| Workstation Embedding | Docker infinity-embed 已停 | **真机 Qwen3-Embedding-4B :9302** | OpenClaw memorySearch 恢复可用 |
| PC01 ComfyUI | 计划任务异常,未加载 NAS | **计划任务修复,extra_model_paths.yaml 生效** | 819 节点,checkpoints 来自 NAS |
| PC02 ComfyUI | 在线 | **extra_model_paths.yaml 生效** | 805 节点,checkpoints 来自 NAS |
| core 角色 | Docker 监控栈(12 容器) | **Docker 全清,PG18+Redis 真机运行** | 改为真机业务服务器,待 ToIV/DRT 迁移 |
| core 监控栈 | 12 容器运行 | **已拆除** | Grafana/VM/Loki/cAdvisor 等全删,监控待重新规划 |
| Cloud SSH | 正常 | **⚠️ banner 超时** | HTTPS 正常,SSH 待排查 |

### 2026-07-27

| 变更项 | 旧状态 | 新状态 | 影响 |
|--------|--------|--------|------|
| ToIV TTS 复测 | 待复测(可能需迁移) | **✅ 测试通过, GPU0 运行(cuda:0)** | /health=ok, /tts 合成 WAV 成功,无需迁移 |
| PC01 ComfyUI 版本 | 0.25.0 | **0.28.0(与 PC02 统一)** | 版本一致,功能对齐 |
| PC01 torch | 2.13.0+cpu(无 CUDA) | **2.13.0+cu130** | RTX 5090 可用,CUDA 13.0 |
| PC01 mihomo | 计划任务 Ready 未运行 | **Running(PID 启动)** | 代理恢复,pytorch.org 可达 |
| PC01 ComfyUI 服务 | 未运行(启动失败) | **✅ Running,监听 :8188** | ComfyUI-LB 5 后端恢复完整 |

### 2026-07-26 晚

| 变更项 | 旧状态 | 新状态 | 影响 |
|--------|--------|--------|------|
| Workstation LLM | Docker SGLang Qwen3.6 | **本机 vLLM 0.26.0 + Nemotron-3-Nano-Omni-30B-A3B BF16** | 全模态(文/图/音/视), GPU3 89GB, served-model-name=qwen3.6-uncensored(向后兼容) |
| ComfyUI-LB 后端 | 6 个(gpu0-3 + pc01/pc02) | **5 个(gpu0/1/2 + pc01/pc02)** | GPU3 让给 Nemotron,Workstation 减压 |
| PC01 ComfyUI | 计划任务存在但未运行 | **已启动,监听 :8188** | 分担 Workstation GPU3 缺位 |
| ToIV TTS | GPU3 | **迁移到 GPU0(cuda:0),systemd 已配置** | 避免与 Nemotron GPU3 冲突 |

---

## 一、集群总览

### 设备清单(17 台 + 1 操作终端)

| # | 设备 | 角色 | LAN IP | Tailscale IP | 系统 | 状态 |
|---|------|------|--------|--------------|------|------|
| 1 | core | ToIV 生产业务机（纯应用层） | 192.168.71.47 | 100.77.80.100 | Ubuntu 26.04 | ✅ toiv-api:8090 / toiv-web:3100 / PG18 / Redis / NAS 挂载全 active |
| 2 | workstation | AI 生成算力 + 真机服务 | 192.168.71.127 | 100.68.100.90 | Ubuntu 26.04 | ✅ 全部推理服务在线 |
| 3 | spark01 | vLLM Ray head (L4 LLM TP=2) | 192.168.71.82 | 100.81.235.124 | Ubuntu (DGX Spark) | ✅ llama-3.3-70b-abliterated，临时顶 L2/L3 |
| 4 | spark02 | vLLM Ray worker (TP=2) | 192.168.71.84 | 100.86.42.89 | Ubuntu (DGX Spark) | ✅ 运行中 |
| 5 | studio01 | EXO RDMA 推理节点 | 192.168.71.109 | 100.67.43.40 | macOS | ❌ Tailscale 离线，EXO 不可用 |
| 6 | studio02 | EXO RDMA 推理节点 | 192.168.71.111 | 100.91.0.121 | macOS | ❌ Tailscale 离线，EXO 不可用 |
| 7 | studio03 | EXO RDMA 推理节点 | 192.168.71.112 | 100.115.27.68 | macOS | ❌ Tailscale 离线，EXO 不可用 |
| 8 | studio04 | EXO RDMA 推理节点 | 192.168.71.113 | 100.126.182.23 | macOS | ❌ Tailscale 离线+磁盘不足(258GB) |
| 9 | openclaw01 | OpenClaw 智能体网关 + 告警微信推送 | 192.168.71.86 | 100.115.23.67 | macOS | ✅ 运行中 |
| 10 | openclaw02 | OpenClaw 智能体网关 | 192.168.71.75 | 100.76.35.7 | macOS | ✅ 运行中 |
| 11 | openclaw03 | OpenClaw 智能体网关 | 192.168.71.81 | 100.76.140.121 | macOS | ✅ 运行中 |
| 12 | openclaw04 | OpenClaw 智能体网关 | 192.168.71.85 | 100.125.217.11 | macOS | ✅ 运行中 |
| 13 | pc01 | ComfyUI worker (RTX 5090) | 192.168.71.115 | 100.69.134.27 | Windows 11 | ✅ 运行中,NAS 模型已加载 |
| 14 | pc02 | ComfyUI worker (RTX 5090) | 192.168.71.114 | 100.107.94.26 | Windows 11 | ✅ :8193 在线(PyTorch 2.13+cu130),NAS 模型已加载;Tailscale 需本地登录 |
| 15 | NAS | SMB 存储 (绿联 DXP8800) | 192.168.71.7 | 100.80.237.96 | UGREEN OS | ✅ core 已挂载 /mnt/toiv-nas |
| 16 | cloud | 网关 / 1Panel / frps | 43.119.32.180 | 100.83.78.114 | Linux (阿里云) | ⚠️ HTTPS 正常,SSH banner 超时 |
| 17 | MateBook | 操作终端 | - | 100.74.15.34 | macOS | ✅ 本机 |

### 重要变更记录(2026-07-28)

| 变更项 | 旧状态 | 新状态 | 影响 |
|--------|--------|--------|------|
| core 角色 | Docker 监控中心(12 容器) | **真机业务服务器(PG18+Redis)** | Docker 已禁用并全清,待 ToIV/DRT 业务上 core |
| Workstation 服务架构 | Docker 混合部署 | **全部真机部署** | ComfyUI/TTS/Nemotron/Embedding 均不再跑 Docker |
| Workstation Embedding | Docker infinity-embed | **真机 Qwen3-Embedding-4B** | :9302 恢复,OpenClaw memorySearch 可用 |
| PC01/PC02 模型来源 | 本地 models | **NAS 模型库** | extra_model_paths.yaml 生效,节省本地磁盘 |
| Cloud SSH | 正常 | **banner 超时** | HTTPS 仍正常,SSH 待排查 |

---

## 二、设备详细说明

### 2.1 core — ToIV 生产业务机（纯应用层）

**硬件**: Ubuntu 26.04 / 4×10GbE 网卡
**角色**: ToIV 生产业务机（仅应用层，无推理）

**当前服务(真机)**:

| 服务 | 端口 | 状态 | 说明 |
|------|------|------|------|
| toiv-api | 8090 | ✅ 运行 | `/openapi.json` 200 |
| toiv-web | 3100 | ✅ 运行 | Web 前端 |
| PostgreSQL 18 | 5432 | ✅ 运行 | 待代码从 SQLite 切换 |
| Redis | 6379 | ✅ 运行 | 127.0.0.1 |
| NAS SMB | /mnt/toiv-nas | ✅ 挂载 | 192.168.71.7 模型存储 |

**SSH**: `ssh merlin@192.168.71.47`

**注意**: 所有 Docker 容器已清理,Docker 服务已禁用。ToIV/DRT 业务以真机 systemd 方式运行。

---

### 2.2 Mac Studio(studio01-04)— EXO RDMA 内存池

**硬件**: Apple M3 Ultra / 512GB 统一内存 / 80 核 GPU / Metal 4
**角色**: EXO RDMA 推理集群(OpenClaw 已清理,专职 EXO)

| 服务 | 端口 | 状态 | 说明 |
|------|------|------|------|
| EXO.app | :52415 (API) + :52416 | ✅ 4 台运行 | RDMA 集群,40+ 模型 |
| mihomo | :7890 / :1053 / :9090 | ✅ 4 台运行 | launchd 托管 com.dgmt.mihomo |
| Tailscale | - | ✅ 4 台在线 | mesh 网络 |
| ~~OpenClaw~~ | ~~:18789~~ | ❌ **已清理** | 2026-07-24 彻底移除 |

**SSH**: `ssh studio0X-lan`(用户名 dgmt-studio01-04)
**IPv6**: autoconf 会轮换,**必须用 LAN IPv4**

**EXO 模型分布**:
- studio01:Kimi-K2.7-Code-4bit + GLM-5.2-fp8 + FLUX.1-schnell
- studio02:Kimi-K2.7-Code-4bit
- studio03:Kimi-K2.7-Code-4bit + GLM-5.2-fp8
- studio04:Kimi-K2.7-Code-4bit(**磁盘仅剩 258GB**)

**EXO 端点**: `http://192.168.71.109:52415/v1`(主节点 studio01)

---

### 2.3 Mac Mini(openclaw01-04)— OpenClaw 智能体网关

**硬件**: Apple M4 / 16GB 统一内存
**角色**: OpenClaw 智能体(唯一正式部署设备)

| 服务 | 端口 | 状态 | 说明 |
|------|------|------|------|
| OpenClaw gateway | :18789 | ✅ 4 台运行 | launchd 托管,KeepAlive |
| mihomo | :7890 | ✅ 4 台运行 | launchd 托管 |
| Tailscale | - | ✅ 4 台在线 | mesh 网络 |
| wechat-bridge(仅 openclaw01) | :9095 | ✅ 运行 | 告警微信推送 |

**SSH**: `ssh openclaw0X-lan`(用户名 dgmt-openclaw01-04)
**Node**: v24.18.0(nvm 管理,路径 `~/.nvm/versions/node/v24.18.0/bin/openclaw`)
**OpenClaw 版本**: 2026.7.1-2

**OpenClaw 配置(4 台一致)**:
- models.providers.qwen36:Workstation :8000 qwen3.6-uncensored(**primary**,实为 Nemotron)
- models.providers.euryale:spark01 Euryale 70B(**fallback**)
- memorySearch: Workstation :9302(Qwen3-Embedding-4B, 2026-07-28 恢复)
- tools.profile=full / security=full / ask=off(YOLO 全开)
- agents 三 workspace:main / dev / ops
- plugins.entries.openclaw-weixin.enabled=true(微信插件启用)

**身份**: 4 台统一为 **Drita**(全能 AI 助手)

**SSH 注意**: non-interactive shell 不加载 .zshrc,需 `export PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH"` 后才能用 openclaw 命令

---

### 2.4 Spark(spark01-02)— L4 LLM 分布式推理（临时顶 L2/L3）

**硬件**: NVIDIA DGX Spark / Grace Blackwell / 128GB 统一内存
**角色**: vLLM 推理后端(llama-3.3-70b-abliterated TP=2 分布式)

| 设备 | 角色 | Ray IP | :8000 | 说明 |
|------|------|--------|-------|------|
| spark01 | Ray head + vLLM HTTP server | 192.168.201.13 | ✅ 监听 | 服务 llama-3.3-70b-abliterated |
| spark02 | Ray worker (TP=2) | 192.168.201.12 | ❌ 不监听 | **正常行为**,worker 不监听 HTTP |

**SSH**: `ssh sk0X-l`(用户名 dgmt-spark)
**vLLM 服务**: Docker 容器 `vllm_node`(host network)
**模型**: llama-3.3-70b-abliterated(BF16, TP=2, max_model_len=32768, max_num_seqs=32, gpu_memory_utilization=0.88, distributed-executor-backend ray)
**推理验证**: `curl http://192.168.71.82:8000/v1/chat/completions` → 200 OK
**性能**: ~90s/300token,纯 content(无 thinking),NSFW 不拒绝

**重要说明**:
- spark02 :8000 未监听是**正常行为**,它是 Ray worker 节点
- 两台通过 Ray 集群(端口 29501)通信,形成 TP=2 分布式推理
- 不要尝试在 spark02 上单独启动 vLLM
- **当前临时承担 L2/L3 角色**:studio01-04 Tailscale 离线导致 EXO 不可用,core .env LLM 端点指向 spark,待 EXO 恢复后回切

---

### 2.5 Workstation — AI 生成算力 + 真机服务

**硬件**: AMD Ryzen Threadripper PRO 9975WX 32C / 183GB RAM / 4× RTX PRO 6000 Blackwell 96GB
**角色**: ComfyUI-LB 负载均衡 + Nemotron 全模态 LLM + ToIV TTS + AI-Omni ASR + Qwen3 Embedding + 真机业务

| 服务 | 端口 | 状态 | 说明 |
|------|------|------|------|
| ComfyUI-LB | :8188 | ✅ 运行 | 5 后端配置，当前实际 4 个可用(gpu0/1/2 + pc01，pc02 修复中) |
| ComfyUI gpu0-2 | :8189-8191 | ✅ HEALTHY | RTX PRO 6000 ×3 |
| ~~ComfyUI gpu3~~ | ~~:8192~~ | ❌ 已停 | GPU3 让给 Nemotron |
| Nemotron vLLM | :8000 | ✅ 运行(本机) | qwen3.6-uncensored(alias), Nemotron-3-Nano-Omni-30B-A3B BF16, GPU3 92GB |
| ToIV TTS (IndexTTS2) | :9200 | ✅ 运行(GPU0, cuda:0) | /health=ok |
| AI-Omni ASR (faster-whisper large-v3) | :9210 | ✅ 运行(GPU2) | /health=ok，ToIV 代码尚未接入 |
| OpenTalking 数字人 | :4403 | ✅ 运行(GPU2) | SenseVoice STT + MuseTalk 驱动，core 探活 reachable=true |
| OpenTalking TTS shim | :19092 | ✅ 运行 | IndexTTS2 适配层 |
| LiveAct worker | :9400 | ✅ 运行(GPU1+2) | SoulX-LiveAct 14B FP4 双卡，全身数字人，实测 4-6FPS |
| coturn (TURN 中继) | :3478(UDP) | ✅ 运行 | 数字人 WebRTC 跨网段中继 |
| Qwen3-Embedding-4B | :9302 | ✅ 运行(GPU1) | sentence-transformers + FastAPI, 输出维度 2560 |
| ~~Infinity Embedding~~ | ~~:9301/:9302~~ | ❌ 已停 | 替换为真机 Qwen3-Embedding-4B |
| ~~Home Assistant~~ | ~~:8211~~ | ❌ 已停 | Docker 已清,待重新规划 |
| ~~Caddy (workstation)~~ | ~~:443/:80~~ | ❌ 已停 | aigc.dgmt.top 暂由 cloud 反代 |
| Docker | - | ❌ 已禁用 | 所有容器已清 |
| Tailscale | - | ✅ 运行 | 100.68.100.90 |
| NAS SMB | /home/merlin/nas_mount | ✅ 挂载 | 192.168.71.7:445 |

**GPU 分配(硬性规则,不可随意更改)**:

| GPU | 服务 | 端口 | 显存占用 |
|-----|------|------|---------|
| GPU0 | ComfyUI #1 | :8189 | ~0.5GB |
| GPU0 | IndexTTS2 | :9200 | ~7.6GB |
| GPU1 | ComfyUI #2 | :8190 | ~0.5GB |
| GPU1 | Qwen3-Embedding-4B | :9302 | ~8.4GB |
| GPU1 | LiveAct worker | :9400 | 双卡 |
| GPU2 | ComfyUI #3 | :8191 | ~0.5GB |
| GPU2 | AI-Omni ASR (faster-whisper large-v3) | :9210 | ~4.9GB |
| GPU2 | OpenTalking (SenseVoice + MuseTalk) | :4403 | ~2GB |
| GPU2 | LiveAct worker | :9400 | 双卡 |
| GPU3 | Nemotron vLLM | :8000 | ~92GB |

**Nemotron 部署详情**(2026-07-28 优化):
- 模型路径: `/home/merlin/models/Nemotron-3-Nano-Omni-30B-A3B-Reasoning-BF16/`
- venv: `/opt/nemotron-venv/`(Python 3.12 + vLLM 0.26.0)
- 启动脚本: `/opt/nemotron-venv/start_nemotron.sh`
- systemd 服务: `nemotron-vllm.service`(enabled,开机自启)
- vLLM 启动参数: `--tensor-parallel-size 1 --gpu-memory-utilization 0.95 --max-model-len 32768 --max-num-seqs 32 --dtype bfloat16 --moe-backend triton --enable-chunked-prefill --enable-prefix-caching --trust-remote-code`
- 关键环境变量: `CUDA_VISIBLE_DEVICES=3 CUDA_HOME=/opt/nemotron-venv/lib/python3.12/site-packages/nvidia/cu13 VLLM_USE_FLASHINFER_SAMPLER=0`
- served-model-name: `qwen3.6-uncensored`(保持向后兼容)
- 实际模型: NVIDIA Nemotron 3 Nano Omni 30B-A3B Reasoning(BF16, MoE 30B-A3B)
- 支持模态: 文本 / 图像 / 音频 / 视频(全模态)

**Qwen3-Embedding-4B 部署详情**(2026-07-28 恢复):
- 模型路径: `/home/merlin/models/Qwen3-Embedding-4B/`
- 服务脚本: `/opt/qwen3-embedding-server.py`
- systemd 服务: `qwen3-embedding.service`
- 端口: `:9302`
- GPU: GPU1(`CUDA_VISIBLE_DEVICES=1`)
- 输出维度: 2560
- API: OpenAI 兼容 `/v1/embeddings` + `/health`

**AI-Omni ASR 部署详情**(2026-07-29 新增):
- 服务: faster-whisper large-v3
- 路径: `/opt/ai-omni-asr/`
- venv: `/opt/ai-omni-asr/`(Python 3.12 + faster-whisper 1.2.1 + FastAPI)
- 服务脚本: `/opt/ai-omni-asr/asr_server.py`
- 运行方式: `screen` 前台会话(`ai-omni-asr`),日志 `/opt/ai-omni-asr/logs/screen.log`
- 端口: `:9210`
- GPU: GPU2(`device_index=2`)
- 显存占用: ~4.9GB
- API: OpenAI 兼容 `POST /v1/audio/transcriptions`,支持 `file`, `model`, `language`, `response_format` 字段
- 启动命令:
  ```bash
  cd /opt/ai-omni-asr && source bin/activate && python asr_server.py
  ```

**OpenTalking 数字人 / coturn 部署详情**(2026-07-30 补齐):
- OpenTalking unified: `/home/merlin/opentalking`(venv `.venv`,启动 `opentalking-unified`),systemd `opentalking.service`,端口 :4403
- TTS shim: `/home/merlin/opentalking/scripts/toiv_tts_shim.py`,systemd `opentalking-tts-shim.service`,端口 :19092(适配 IndexTTS2 :9200)
- STT: SenseVoiceSmall(NAS `toiv/funasr/SenseVoiceSmall`,GPU2);驱动: MuseTalk(`/home/merlin/models/musetalk`,GPU2)
- coturn: `apt install coturn`,配置 `/etc/turnserver.conf`(lt-cred-mech, realm=toiv, user=toiv, 凭据见 opentalking `.env` `OPENTALKING_WEBRTC_TURN_*`),systemd `coturn.service` enabled
- **为什么需要 TURN**:浏览器(Safari) WebRTC host candidate 用 mDNS `.local` 混淆,跨子网(用户 .31 ↔ 集群 .71)aiortc 无法解析;srflx 公网候选也不通 → ICE 全挂。TURN 监听 LAN IP,浏览器可达即可中继。
- 验证脚本: ToIV 仓库 `scripts/webrtc_e2e_test.py`(需 `pip install aiortc`),实测 .31→.71 CONNECTED + 音视频轨

**ToIV 新模型存放清单（NAS `toiv/comfyui-models`，2026-07-30 逐项核实存在）**:

| 类别 | 内容 | 路径 | 大小 |
|------|------|------|------|
| 文本编码器 | Qwen3-VL-8B-Instruct（目录形式，Qwen-Image 2.0 用） | `text_encoders/qwen_3_vl_8b_instruct/` | text_encoders 共 21G |
| PuLID | pulid_flux_v0.9.0/v0.9.1 + ip-adapter_pulid_sdxl_fp16 | `pulid/` | 3.4G |
| IPAdapter | FaceID Plus v2 sd15/sdxl + 配套 LoRA + plus-face | `ipadapter/` | 2.9G |
| CLIP Vision | EVA02_CLIP_L_336_psz14_s6B.pt（PuLID 依赖） | `clip_vision/` | 817M |
| 音频生成 | ACE-Step 1.5（acestep-v15-turbo + 5Hz-lm-1.7B + vae） | `audio/Ace-Step1.5/` | audio 共 9.9G |
| 场景 LoRA | 42 个，10 分类目录（古风/都市/校园/豪车/特效/恐怖/喜剧/历史战争/运镜/导演风格） | `loras/<分类>/` | ≈13.8G |
| 音频分离 | UVR5 模型（5_HP-Karaoke / Kim_Vocal_2.onnx / DeEcho-DeReverb / MDX-NET-Inst_1） | `audio/uvr5_models/` | — |
| 音频分离(本机) | Demucs v4 htdemucs venv + 模型 | workstation `/home/merlin/toiv-scripts/audio-sep-venv`、`demucs-models`、`application_data/demucs_model_data` | venv 5.5G |
| LTX-2.3 底座 | ltx-2.3-22b-dev bf16 + distilled-1.1（checkpoints/ 与 diffusion_models/ 双份） | `checkpoints/`、`diffusion_models/` | 43G×2×2 |
| LTX-2.3 配套 | 蒸馏提速 LoRA 384-1.1、spatial-x2-1.1/temporal-x2 上采样、IC-LoRA Union/Motion-Track/**LipDub 唇形**、**Foley-V2A 音效**、Cameraman v2 运镜、NSFW motion ×2（Sulphur better motion + NSFWMOTION_00750，Civitai） | `loras/ltx2.3/`、`upscale_models/` | ~10G |
| ACE-Step(本机) | ace-step-venv + checkpoints（下载脚本 `toiv-scripts/download_p0_*.py`） | workstation `/home/merlin/toiv-scripts/` | — |

**⚠️ ToIV 新模型接入缺口（2026-07-30 核查，~~未解决~~ → 当日已解决）**:
1. ~~worker 模型路径不含新模型~~ → **已解决（2026-07-30）**:`/opt/ComfyUI/extra_model_paths.yaml` 新增 `toiv` 段指向 `/home/merlin/nas_mount/toiv/comfyui-models`;pc01/pc02 `C:\ComfyUI\extra_model_paths.yaml` 同步追加（`Z:/toiv/comfyui-models`）。实测 5 台 worker 均可见 `qwen_3_vl_8b_instruct`、42 场景 LoRA、LTX-2.3 全套。
2. ~~PuLID/IPAdapter 自定义节点未安装~~ → **已安装（2026-07-30）**:`ComfyUI-PuLID-Flux` + `ComfyUI_IPAdapter_plus`（workstation /opt/ComfyUI/custom_nodes,venv 补 facexlib/insightface/onnxruntime-gpu/ftfy/timm,ensurepip 先补 pip）。实测节点加载成功，pulid_flux_v0.9.1/EVA02_CLIP_L/faceid-plusv2/antelopev2 全部可见。**注意**:PuLID-Flux v0.9.1 适配 FLUX.1 dev/schnell,**不适用 FLUX.2**（主库目前只有 FLUX.2，用 PuLID-Flux 需另下 FLUX.1 dev);SD1.5/SDXL 角色一致性走 IPAdapter FaceID 路径（底模充足）。
3. **ACE-Step 现状**：原生节点存在（ComfyUI 0.27 内置），主库有旧版 `checkpoints/ace_step_v1_3.5b.safetensors` 可用；新 1.5 turbo 在 toiv 路径已可见，待工作流验证。
4. **LTX-2.3 注意事项（2026-07-30 实测）**:LTXV 加载器扫 `checkpoints/`、UNETLoader 扫 `diffusion_models/`,22B 权重两处都需可见（SMB 不支持软链，已 NAS 本地复制双份）;distilled-1.1 的 gemma norm_type 是大写 `PER_TOKEN_RMS`,workstation `ComfyUI-LTXVideo/text_embeddings_connectors.py` 已打大小写兼容补丁（备份 `.bak.20260730`);LTX 节点仅 workstation 有,LTX 任务只会路由到 gpu0-2。

**SSH**:
- Tailscale:`ssh workstation`(走 100.68.100.90,推荐)
- LAN:`ssh workstation-lan`(走 192.168.71.127)
- 用户名:merlin

**网络状态(重要)**:
- 主网卡 enp210s0f0np0 **状态 DOWN**
- 副网卡 enp210s0f1np1 在用,LAN IP 192.168.71.127
- **旧 IP 192.168.71.100 和 Tailscale 100.99.181.103 已失效**

---

### 2.6 PC(pc01-02)— ComfyUI GPU 节点(分担 Workstation 压力)

**硬件**: RTX 5090 32GB / 128GB RAM / Windows 11

| 设备 | ComfyUI 端口 | 版本 | 状态 |
|------|-------------|------|------|
| pc01 | :8188 | 0.28.0 | ✅ 运行中,NAS 模型已加载(819 节点) |
| pc02 | :8193 | 0.28.0 | ✅ 运行中,PyTorch 2.13+cu130,NAS 模型已加载 |

**SSH**: `ssh pc01`(用户 home) / `ssh pc02`(用户 w)

**pc01/pc02 配置**:
- ComfyUI-LB 第 4/5 后端
- 计划任务 "StartComfyUI" 开机自启
- `extra_model_paths.yaml` 指向 `Z:/Windows/ComfyUI/ComfyUIModel` + `Z:/toiv/comfyui-models`
- Windows 凭据管理器保存 NAS 凭据
- 登录时自动挂载 `Z: \\192.168.71.7\NAS`

**负载分担策略**:ComfyUI 后端共 5 个(gpu0/1/2 + pc01/pc02)，GPU3 留给 Nemotron。当前 pc02 离线，实际 4 后端分担，pc02 修复后恢复 5 个。

---

### 2.7 NAS — 绿联 DXP8800

**硬件**: 44TB 存储(已用 4.1TB, 10%)
**角色**: SMB 文件共享

| 服务 | 端口 | 状态 |
|------|------|------|
| UGREEN Web 管理 | :5443 | ✅ |
| SMB | :445 | ✅ 运行 |
| Redis | :6379 (本地) | ✅ |
| PostgreSQL | :5432 (本地) | ✅ |
| Tailscale | - | ✅ (100.80.237.96) |

**SSH**: `ssh nas`(用户名 dgmt-nas,无 sudo 免密)
**SMB 凭据**: `dgmt-nas / Aki.19950108`
**挂载路径**: Mac Studio `~/NAS`,Workstation `/home/merlin/nas_mount`,PC01/PC02 `Z:`

**关键模型路径**:
- `NAS/Windows/ComfyUI/ComfyUIModel/models` — 主模型库(524GB)
- `NAS/toiv/comfyui-models` — ToIV 专用模型(~180GB)

---

### 2.8 cloud — 阿里云网关

**硬件**: 49GB 磁盘(已用 21GB, 44%)
**角色**: 网关 / 1Panel / frps 内网穿透 / mihomo 代理

| 服务 | 端口 | 状态 |
|------|------|------|
| openresty | :80/:443 | ✅ Web 入口 |
| 1Panel | :1212 | ✅ 管理面板 |
| aigc-auth | :8001 | ✅ AIGC 认证 |
| exo-proxy | :8080 | ✅ EXO 代理 |
| mihomo | :7890/:9090 | ✅ 代理 |
| frps | :7000/:7500 | ✅ 内网穿透 |

**SSH**: `ssh cloud`(43.119.32.180 或 100.83.78.114,用户 root)

**注意**: 2026-07-28 SSH 出现 banner 超时,HTTPS 正常,待排查(可能 fail2ban 或 1Panel 安全策略)。

---

## 三、监控系统

### 3.1 状态说明(2026-07-28)

core 上原 Docker 监控栈(Grafana/VM/Loki/Alertmanager 等)已全清,监控系统待重新规划。

### 3.2 告警通知链路(已停用)

原链路:
```
vmalert (core:8880) → Alertmanager (core:9093) → wechat-bridge (openclaw01:9095) → openclaw agent main → 微信
```

当前状态: core 监控栈已拆除,该链路暂时不可用。openclaw01 上 wechat-bridge 服务本身仍在运行。

---

## 四、AICG 四层模型流水线

> 详见 `/Users/wangzhenyu/Desktop/ALLProject/AICG-模型接入配置.md`
>
> **⚠️ 2026-07-30 状态**:Mac Studio(studio01-04)全部 Tailscale 离线，EXO 不可用，**L2/L3 临时降级走 spark**(llama-3.3-70b-abliterated)，精修质量暂时受 L1 限制。

| 层 | 用途 | 设备 | 端点 | 模型 ID | 速度 | NSFW |
|----|------|------|------|---------|------|------|
| L1 初稿 | 实时交互(全模态) | Workstation | 192.168.71.127:8000 | qwen3.6-uncensored(Nemotron-3-Nano-Omni-30B-A3B BF16) | 1-3s | ✓ |
| L2 主力 | 关键场景润色 | ~~Mac Studio EXO~~ → **spark(临时)** | ~~192.168.71.109:52415~~ → 192.168.71.82:8000 | ~~Kimi-K2.7-Code-4bit~~ → **llama-3.3-70b-abliterated** | ~~6.6s~~ → ~90s | ✓ |
| L3 精修 | 异步批量终稿 | ~~Mac Studio EXO~~ → **spark(临时)** | 同上 | ~~GLM-5.2-fp8~~ → **llama-3.3-70b-abliterated** | ~~115s~~ → ~90s | ✓ |
| L4 NSFW | 无审查内容 | Spark01+02 | 192.168.71.82:8000 | llama-3.3-70b-abliterated | ~90s | ✓ |

**配套服务**:
- TTS:Workstation :9200(IndexTTS2)
- 图像:Workstation :8188(ComfyUI-LB，当前 4 后端：gpu0/1/2 + pc01，pc02 修复中)
- EXO 图像:Mac Studio :52415 当前不可用（studio 离线）
- Embedding:Workstation :9302(Qwen3-Embedding-4B)
- ASR:Workstation :9210(faster-whisper large-v3)
- 数字人:Workstation :4403(OpenTalking + MuseTalk)
- 数字人直播:Workstation :9400(LiveAct 14B FP4)
- TURN 中继:Workstation :3478(coturn)

---

## 五、服务依赖矩阵

| 服务 | 运行设备 | 端口 | 依赖 | 影响范围 |
|------|---------|------|------|---------|
| vLLM (llama-3.3-70b) | spark01+02 | :8000 (spark01) | Ray TP=2, NCCL | OpenClaw 4 MacMini + AICG L4 + 临时 L2/L3 |
| OpenClaw gateway | 4 MacMini | :18789 | vLLM :8000 | Agent 执行 |
| EXO.app | 4 MacStudio | :52415 | 无 | 本地推理(AICG L2/L3，**当前不可用**) |
| Nemotron vLLM | Workstation | :8000 | GPU3 92GB | OpenClaw primary + AICG L1 |
| ComfyUI-LB | Workstation | :8188 | 后端健康(当前 4 个) | 图像/视频生成 |
| IndexTTS2 | Workstation | :9200 | GPU0 | TTS |
| AI-Omni ASR | Workstation | :9210 | GPU2 | ASR(ToIV 待接入) |
| OpenTalking | Workstation | :4403 | GPU2 + TTS shim :19092 | 数字人 |
| LiveAct worker | Workstation | :9400 | GPU1+2 | 数字人直播 |
| Qwen3-Embedding-4B | Workstation | :9302 | GPU1 | OpenClaw memorySearch |
| coturn TURN | Workstation | :3478 | 无 | WebRTC 跨网段中继 |
| toiv-api/toiv-web | core | :8090/:3100 | PG+Redis+推理端点 | ToIV 生产业务 |
| NAS SMB | NAS | :445 | 网络 | 模型/数据存储 |
| cloud openresty | cloud | :80/:443 | frps | 公网入口 |

---

## 六、关键凭据与挂载

> ⚠️ 这些凭据已多次确认,**禁止再次询问用户**

| 服务 | 用户名 | 密码 | 备注 |
|------|--------|------|------|
| NAS SMB | dgmt-nas | Aki.19950108 | 192.168.71.7,共享名 NAS |
| Tailscale Auth Key | — | tskey-auth-kPM5hHvNGY11CNTRL-UTn8rtRjK8Pfw3riNoGB8Pru71VhdRR9C | 已用于 core 设备授权 |

### NAS 挂载方式

**Linux (Workstation)**: 已配置 fstab 自动挂载到 `/home/merlin/nas_mount`
```bash
# 凭据文件:/root/.smbcredentials
username=dgmt-nas
password=Aki.19950108
```

**Windows (PC01/PC02)**: 用 `cmdkey` + `schtasks` 自动挂载
```cmd
cmdkey /add:192.168.71.7 /user:dgmt-nas /pass:Aki.19950108
net use Z: \\192.168.71.7\NAS /persistent:yes
```

---

## 七、易错点(2026-07-28 教训)

1. **GPU 分配搞错**: GPU3 是 Nemotron vLLM 专用,TTS 必须在 GPU0
2. **/tmp tmpfs 吃内存**: 大文件打包应使用 `/var/tmp` 或磁盘目录,避免写入 /tmp
3. **Windows SSH session 隔离**: SMB 映射是 per-session 的,需用 cmdkey + 计划任务实现登录自动挂载
4. **ComfyUI-LB 后端数量**: 当前 5 个后端(gpu0/1/2 + pc01/pc02),GPU3 不跑 ComfyUI
5. **不要通过 SSH 启动 Windows 长期进程**: 应使用计划任务 + bat 脚本,LogonType=InteractiveToken
6. **禁止重复询问 NAS 密码**: 已记录在本文件第六节

---

## 八、待办事项

- [x] style_presets.py LoRA 语法 bug 修复（✅ 已完成：去 `<lora:>` 语法，加 loras 字段，7 个兼容 LoRA 正式生效）
- [x] **pc02 PyTorch cu130 升级 + ComfyUI 重启**（✅ 已完成：PyTorch 2.13+cu130 + extra_model_paths.yaml YAML 缩进修复，:8193 在线，LB 恢复 5 后端）
- [ ] pc02/spark02 Tailscale 恢复（到家后手动启动/重连，SSH 无法完成 GUI 认证；不影响当前使用）
- [ ] **FLUX.1 dev 下载**（PuLID-Flux 底模依赖;HF black-forest-labs gated,token 权限已具备）
- [ ] 角色一致性端点实现(IPAdapter FaceID @ SDXL / PuLID-Flux @ FLUX.1,节点已装)
- [ ] studio01-04 Tailscale 恢复（到家后手动启动 Tailscale App，EXO L2/L3 才能回切）
- [ ] luxury_business/special_effects/director_style 三个场景预设的图像兼容 LoRA 验证
- [ ] Cloud SSH banner 超时排查(HTTPS 正常)
- [ ] Cloud 反代切换指向 core(待 core 业务就绪后)
- [ ] 监控系统重新规划
